

new eProcessor('balancerequest', {
    process: function(self, db) {
        clearEvents(self).onclick = f() {
            var view = VIEW.balancerequest();
            POP.modal.show(view);
            
            return 0;
        }
    }
});















