//-new eProcessor('userSettings', {
//-	process(self, db) {
//-		if ( EVENT.data.windowSize.x <= %wrapperRatesMobile ) {
//-			document.body.addCls('noScroll');
//-		}
//-		
//-		evt(S('.wrapper')[0], 'click', function() {
//-			document.body.remCls('noScroll');
//-		});
//-	},
//-	singleProcess: false
//-});
